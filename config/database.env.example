# 数据库类型：sqlite / postgres
DB_TYPE=sqlite

# 默认本地测试使用 SQLite 文件数据库
# 当 DB_TYPE=sqlite 时使用
SQLITE_PATH=./storage/app.db

# 当 DB_TYPE=postgres 时使用
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=sales_support_agent
DB_USER=postgres
DB_PASSWORD=your_password_here